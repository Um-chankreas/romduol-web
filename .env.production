# Used only for `npm run build` (production build).
# Frontend talks to the backend through the reverse proxy on the same origin.
VITE_API_BASE_URL=https://api.romduolscholars.com/api
VITE_AGORA_APP_ID=90f0e5a8c82643fcb78693895d820267
